{
  "name": "wechat-gui-check",
  "version": "0.1.0",
  "type": "commonjs",
  "license": "MIT",
  "description": "Config-driven GUI smoke checks for WeChat Mini Program repositories.",
  "scripts": {
    "check": "node check.js",
    "check:fixture": "node check.js --config examples/sample.route-config.json --route home",
    "check:fixture:dry-run": "node check.js --config examples/sample.route-config.json --route home --dry-run"
  },
  "dependencies": {
    "pngjs": "^7.0.0"
  }
}
