# v0.2 Execution Checklist

This document turns the public roadmap into an execution-ready checklist for the `v0.2` cycle.

## Goal

Move the repository from a docs-first public beta into a more validated and repeatable release by strengthening:

- skill usage evidence
- repository-level validation and contributor workflows
- the forward-tested confidence of `tools/wechat-gui-check`

## Definition of Done

`v0.2` is ready when all of the following are true:

- every active skill has example prompts and at least one recorded prompt validation pass
- the skill validator checks meaningful content requirements, not only file presence
- contributors can run a single local validation entry point
- CI covers skill content, docs hygiene, and tool smoke checks
- `tools/wechat-gui-check` has at least one documented forward-test on a public miniapp repo outside this repository
- release notes and changelog updates are ready for tagging

---

## Milestone M1 - Skill Usage Evidence

### M1.1 Add example prompts for every skill

- [x] Create `skills/miniapp-devtools-cli-repair/references/example-prompts.md`
- [x] Create `skills/miniapp-devtools-gui-check/references/example-prompts.md`
- [x] Create `skills/miniapp-devtools-recovery/references/example-prompts.md`
- [x] Create `skills/miniapp-official-scaffold-alignment/references/example-prompts.md`
- [x] Each file includes at least:
  - [x] 2 realistic user prompts
  - [x] expected answer structure or evaluation notes
  - [x] one "do not use this skill when..." example
- [x] Link or mention these examples from the relevant skill docs if helpful

**Acceptance criteria**

- reviewers can understand how each skill should be invoked without guessing
- example prompts are generic and not tied to a private product repo

### M1.2 Add a skill review checklist

- [ ] Create `docs/skill-review-checklist.md`
- [ ] Include checks for:
  - [ ] trigger clarity
  - [ ] procedural quick start quality
  - [ ] output format clarity
  - [ ] repo-specific leakage
  - [ ] reference usefulness
  - [ ] example-prompt coverage

**Acceptance criteria**

- the checklist is short enough to use during normal PR review
- contributors can apply it without tribal knowledge

### M1.3 Record prompt-level validation passes

- [x] Create `docs/skill-validation-log.md`
- [x] Record at least one validation pass for each active skill
- [x] For each validation entry, capture:
  - [x] prompt used
  - [x] expected behavior
  - [x] observed behavior
  - [x] gaps or follow-up actions

**Acceptance criteria**

- each skill has at least one evidence entry before v0.2 release
- open issues are visible instead of implied

---

## Milestone M2 - Repository Validation and Contributor Workflow

### M2.1 Upgrade `scripts/validate_skills.py`

- [x] Check that every public skill contains a `references/` directory
- [x] Check that every `SKILL.md` contains these sections:
  - [x] `## Overview`
  - [x] `## Quick Start`
  - [x] `## Core Rules`
  - [x] `## Output Format`
- [x] Check that `agents/openai.yaml` exists and includes the required interface structure
- [x] Check that frontmatter `name` matches the folder name
- [x] Check that frontmatter `description` is present and meaningful
- [x] Optionally fail if `references/example-prompts.md` is missing once M1 is adopted
- [x] Improve failure messages so contributors know exactly what to fix

**Acceptance criteria**

- the validator catches real content regressions, not just missing files
- CI output is actionable without opening the script source

### M2.2 Add a single local validation entry point

- [ ] Add one of the following:
  - [ ] `scripts/check.ps1`
  - [ ] `Makefile`
  - [ ] `justfile`
- [ ] The entry point runs the core checks in a predictable order
- [ ] Include at least:
  - [ ] skill validation
  - [ ] JSON sample validation
  - [ ] tool syntax checks
  - [ ] `tools/wechat-gui-check` dry-run smoke check
- [ ] Document the command in `README.md`

**Acceptance criteria**

- a contributor can run one command before opening a PR
- the command works from a clean checkout with documented prerequisites

### M2.3 Expand CI coverage

- [ ] Update `.github/workflows/validate.yml` to use the new single-entry workflow where practical
- [ ] Add checks for:
  - [ ] example prompt presence
  - [ ] markdown or docs hygiene
  - [ ] YAML/JSON validity where relevant
  - [ ] repo-owned smoke coverage for `wechat-gui-check`
- [ ] Keep the workflow honest about host-only limitations of real GUI automation

**Acceptance criteria**

- CI prevents accidental regression in skills, docs, and tool scaffolding
- CI does not pretend to verify host-only behavior it cannot actually verify

---

## Milestone M3 - GUI Checker Confidence

### M3.1 Document a forward-test on an external public miniapp repo

- [ ] Create `docs/gui-check-forward-test.md`
- [ ] Run `tools/wechat-gui-check` against at least one public miniapp repo outside this repository
- [ ] Record:
  - [ ] repo used
  - [ ] host environment
  - [ ] DevTools version if known
  - [ ] dry-run result
  - [ ] live-run result
  - [ ] report summary
  - [ ] blockers or follow-up work

**Acceptance criteria**

- there is written evidence that the harness generalizes beyond the bundled fixture
- limitations are explicit and reproducible

### M3.2 Standardize failure classification in reports

- [ ] Review current `report.json` output fields
- [ ] Define a stable failure classification scheme, for example:
  - [ ] `environment_error`
  - [ ] `automation_dependency_missing`
  - [ ] `devtools_session_error`
  - [ ] `repo_runtime_error`
  - [ ] `selector_assertion_error`
  - [ ] `screenshot_timeout`
- [ ] Update sample output docs if the schema changes
- [ ] Make README language match the actual report fields

**Acceptance criteria**

- contributors can tell whether a failure belongs to host setup, DevTools session state, or repo code
- report interpretation is consistent across runs

### M3.3 Improve GUI checker documentation

- [ ] Update `tools/wechat-gui-check/README.md`
- [ ] Clarify the difference between:
  - [ ] dry-run
  - [ ] preflight readiness
  - [ ] live automation
- [ ] Document recommended `miniprogram-automator` installation patterns
- [ ] Add a short troubleshooting section for common failures

**Acceptance criteria**

- a new contributor can reach a successful dry-run using only the README
- automator dependency expectations are easy to understand

### M3.4 Add a richer sample route config if needed

- [ ] Review whether `examples/sample.route-config.json` is enough for future maintenance
- [ ] If not, add a second sample that covers more than one route or action type
- [ ] Keep samples generic and safe for public review

**Acceptance criteria**

- sample coverage is broad enough to exercise future refactors safely

---

## Milestone M4 - Release Preparation

### M4.1 Update roadmap and release-facing docs

- [x] Update `docs/public-roadmap.md` with the v0.2 state
- [x] Update `CHANGELOG.md`
- [x] Ensure README still describes the repository honestly after the new checks land

**Acceptance criteria**

- the published docs match the actual state of the repository
- there is no hidden release knowledge that only maintainers know

### M4.2 Draft the v0.2 release note

- [ ] Create `docs/release-v0.2.0-draft.md`
- [ ] Summarize:
  - [ ] what improved for skill consumers
  - [ ] what improved for contributors
  - [ ] what is still intentionally beta
  - [ ] what will likely move into v0.3

**Acceptance criteria**

- maintainers can tag a release without reconstructing context from PR history

---

## Suggested Issue Breakdown

If you want to turn this into GitHub issues, these are good issue-sized units:

1. add example prompts for all four skills
2. add skill review checklist and validation log
3. upgrade `scripts/validate_skills.py`
4. add a single local validation entry point and document it
5. expand CI coverage around docs and examples
6. document external forward-testing for `wechat-gui-check`
7. standardize `report.json` failure classification
8. improve `tools/wechat-gui-check/README.md`
9. prepare changelog, roadmap, and v0.2 release draft

---

## Recommended Execution Order

1. finish M1 first so the repository has real usage evidence
2. implement M2 next so the new expectations become enforceable
3. complete M3 once the validation baseline is stable
4. close with M4 so the release story matches the repository state

---

## Owner / Status Table

Use this section as a lightweight coordination board.

| Work item | Owner | Status | Notes |
| --- | --- | --- | --- |
| M1.1 example prompts | unassigned | completed | |
| M1.2 review checklist | unassigned | not started | |
| M1.3 validation log | unassigned | completed | all 4 active skills have recorded passes |
| M2.1 validator upgrade | unassigned | completed | |
| M2.2 single local check entry | unassigned | not started | |
| M2.3 CI expansion | unassigned | not started | |
| M3.1 external forward-test | unassigned | not started | |
| M3.2 failure classification | unassigned | not started | |
| M3.3 README improvements | unassigned | not started | |
| M3.4 richer sample config | unassigned | not started | |
| M4.1 roadmap/changelog update | unassigned | completed | roadmap, changelog, and README now reflect the current v0.2 state |
| M4.2 release draft | unassigned | not started | |
